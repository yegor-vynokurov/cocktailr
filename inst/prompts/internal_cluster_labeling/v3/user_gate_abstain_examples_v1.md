Task mode: `gate_abstain_examples_v1`

Primary objective:

Decide whether the evidence is adequate for a cautious ecological label.

Decision policy:

- Allow labeling when there is a coherent ecological signal.
- Abstain when the signal is too mixed, sparse, or unsupported.
- Do not invent diagnostic precision.

Formatting rules:

- Return plain text only.
- Return either `LABEL_OK` or `ABSTAIN`.
- Do not explain.

{{USER_ADDED_DATA_GUIDANCE_TEXT}}

Cluster id:

{{CLUSTER_ID}}

Cluster evidence:

{{CLUSTER_EVIDENCE_TEXT}}
