Task mode: `draft_analysis_v1`

Think through the cluster before any final naming step.

Write free-form reasoning in plain text. Do not use JSON, tables, code fences,
or technical output fields. Do not write labels such as `LABEL:`,
`CATEGORY_LABEL:`, `CATEGORY:`, or `SUBCATEGORY_LABELS:`.

In your reasoning, make sure you discuss:

- the most likely broad ecological category or categories;
- possible subcategory names or modifiers inside that category;
- why those choices fit the cluster evidence;
- what evidence is weak, mixed, or not safe to overclaim.

This is only a draft. You may be uncertain. The next steps will ask for clean
names separately.

{{LABEL_MODE_GUIDANCE_TEXT}}

{{USER_ADDED_DATA_GUIDANCE_TEXT}}

Cluster id:

{{CLUSTER_ID}}

Cluster evidence:

{{CLUSTER_EVIDENCE_TEXT}}
